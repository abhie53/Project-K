import React from "react";

const Button = (props) => {
    return(
        <div className={props.className}>
            <button>CLICK ME</button>
            <p>My name is sudarshan shinde</p>
        </div>
    )
}

export default Button;